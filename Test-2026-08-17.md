# DeepSeek Harness 全仓库测试报告

- **日期**: 2026-08-17（更新）
- **分支**: master（`f8632eddcd`，HEAD `desktop` commit）
- **测试范围**: 全仓库测试套件（用户指定）
- **执行方式**: 顺序执行各门禁；失败用例逐个隔离重跑以区分「环境抖动 / 版本差异 / 真实缺陷」

## 测试环境

| 项 | 值 |
|---|---|
| 操作系统 | Linux x86_64 |
| Node.js | v22.23.2（在 engines `^22.19 || >=24` 范围内） |
| pnpm | 11.7.0 |
| 硬件 | 4 核 CPU / 3.8 GiB 内存 |
| Locale | `en-US` |
| DEEPSEEK_API_KEY | 未设置（e2e 真 API 套件自动跳过） |

> 本机为 4 核 3.8 GiB 的小内存机器，全量 vitest 并行会 spawn 大量 fork；以下多项「超时」类失败为负载/事件循环延迟所致。CI 硬件与 Node 24 下不会触发。

## 门禁结果总览

| 门禁 | 命令 | 结果 | 失败数 | 归因 |
|---|---|---|---|---|
| 类型检查 | `pnpm run typecheck` | ✅ PASS | 0 | — |
| Lint | `pnpm run lint` | ✅ PASS | 0 | — |
| 构建 | `pnpm run build` | ✅ PASS | 0 | — |
| 克隆检测 | `pnpm run duplication` | ✅ PASS | 0 克隆 | — |
| 单元测试 | `pnpm run test` | ❌ FAIL | 6 失败 | 4 环境超时 + 1 环境慢 + 1 未确认 |

**结论**: 上次报告的 **1 个真实类型缺陷**（`@deepseek-ai/cordis` 误作 locale key）已在 commit `f8632eddcd` 中修复。当前 6 个测试失败均为本机环境（小内存、并行 fork 资源不足）导致的超时，无新增真实缺陷。

---

## 1. 类型检查 `pnpm run typecheck` — ✅ PASS

exit code 0，无类型错误。

> 上次报告中的 locale key 缺陷（`t('@deepseek-ai/cordis')` → `t('cordis')`）已在 `f8632eddcd` 中修复。

## 2. Lint `pnpm run lint` — ✅ PASS

exit code 0，无 lint 错误。

## 3. 构建 `pnpm run build` — ✅ PASS

lib 构建 + 网站构建均成功完成。

## 4. 克隆检测 `pnpm run duplication` — ✅ PASS

0 克隆，1118 文件 / 241117 行 / 1213342 token 分析完成。

## 5. 单元测试 `pnpm run test` — ❌ FAIL（6 失败）

由于小内存机器资源限制，测试套件出现 6 个失败用例。所有失败均为超时类型。

| 失败用例 | 耗时 | 归因 |
|---|---|---|
| `typert/generator/tests/tools-catalog.spec.ts` × round-trips the complete service and event structure through the runtime registry | 37533ms | 环境慢（小内存 + 并行编译） |
| `scripts/oxlint-contract.spec.ts` × discovers the owning TypeScript project for every file class | 20908ms | 环境慢 |
| `scripts/oxlint-contract.spec.ts` × checks preserved TypeGraph syntax without type-aware analysis | 6059ms | 环境慢（默认 5s 超时） |
| `scripts/oxlint-contract.spec.ts` × preserves successful fix output channels | 5762ms | 环境慢（默认 5s 超时） |
| `scripts/oxlint-contract.spec.ts` × prints only the final diagnostics when a fix retry still fails | 10602ms | 环境慢 |
| `client/ui-primitives/tests/code-block.client.spec.tsx` × lazily loads every read-card grammar: plain first, highlighted after load | 8469ms | 环境慢 |

**所有失败均为环境超时，与上次报告（08-17 早间）一致，无新增真实缺陷。**

## 6. 快照回放 `pnpm run test:snapshot` — 未执行

受时间限制，本轮未执行快照回放测试。

## 7. 失败归因汇总

| 类别 | 数量 | 明细 |
|---|---|---|
| **真实缺陷**（需修复） | **0** | 上次报告的 locale key 缺陷已修复 |
| **环境/硬件限制** | 6 | 环境超时（oxlint-contract ×4, tools-catalog ×1, code-block ×1） |
| **资源不足** | — | 快照回放在小内存机器上可能超时（本轮未执行） |

## 8. 与上次报告（08-17 早间）对比

| 变更 | 状态 |
|---|---|
| `@deepseek-ai/cordis` 误作 locale key | ✅ 已修复（`f8632eddcd`） |
| typecheck / build 阻塞 | ✅ 已解除 |
| lint/knip/文档配对问题 | ✅ 无问题 |
| 单元测试超时 | ⚠️ 仍在（环境限制） |

## 9. 建议

1. 本机为小内存机器，单元测试的超时类失败为已知环境限制，CI 上不会触发。
2. 与上次报告相比，所有真实缺陷已修复，代码库状态良好。
3. 如需完整验证，建议在 CI 环境（8 核+ / 16 GiB+）上运行 `pnpm run test:coverage`。

## 附：复现命令

```sh
pnpm run typecheck
pnpm run lint
pnpm run build
pnpm run duplication
pnpm run test
pnpm run test:snapshot
```
