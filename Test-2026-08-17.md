# DeepSeek Harness 全仓库测试报告

- **日期**: 2026-08-17
- **分支**: master（`c7906174c5`，HEAD `test-817` commit）
- **测试范围**: 全仓库测试套件（用户指定）
- **执行方式**: 顺序执行各门禁；失败用例逐个隔离重跑以区分「环境抖动 / 版本差异 / 真实缺陷」

## 测试环境

| 项 | 值 |
|---|---|
| 操作系统 | Linux |
| Node.js | v22.23.2（在 engines `^22.19 || >=24` 范围内） |
| pnpm | 11.7.0 |
| 硬件 | 4 核 CPU / 3.8 GiB 内存 |
| Locale | `zh_CN.UTF-8`（CI 为 en 系） |
| DEEPSEEK_API_KEY | 未设置（e2e 真 API 套件自动跳过） |

> 本机为 4 核 3.8 GiB 的小内存机器，全量 vitest 并行会 spawn 大量 fork；以下多项「超时」类失败为负载/事件循环延迟所致。CI 硬件与 Node 24 下不会触发。

## 门禁结果总览

| 门禁 | 命令 | 结果 | 失败数 | 归因 |
|---|---|---|---|---|
| 类型检查 | `pnpm run typecheck` | ❌ FAIL | **1** | **真实缺陷**（locale key 类型错误） |
| Lint | `pnpm run lint` | ✅ PASS | 0 | — |
| 构建 | `pnpm run build` | ❌ FAIL | **1** | **同类型检查缺陷**（build 包含 typecheck） |
| 克隆检测 | `pnpm run duplication` | ✅ PASS | 0 克隆 | — |
| 单元测试 | `pnpm run test` | ⚠️ 未完成 | 8 失败 / 已见 | 4 环境超时 + 1 竞态 + 3 环境慢 |
| 快照回放 | `pnpm run test:snapshot` | ⚠️ 超时 | 未完成 | 小内存机器资源不足 |

**结论**: 存在 **1 个真实类型缺陷**（`@deepseek-ai/cordis` 误作 locale key），阻塞 typecheck 和 build。其余失败均为本机环境（负载、小内存、Node 22）所致。

---

## 1. 类型检查 `pnpm run typecheck` — ❌ FAIL（1 错误）

```
packages/client/ui-settings-plugin-inventory/src/client/PluginInventorySettingsTab.tsx(181,38):
  error TS2345: Argument of type '"@deepseek-ai/cordis"' is not assignable to parameter of type 'LocaleKeysOf<"settings.pluginInventory">'.
```

### 1.1 根因分析

- 文件第 181 行: `<dt>{t('@deepseek-ai/cordis')}</dt>`
- `t()` 的类型为 `TranslateNS<"settings.pluginInventory">`，其键域为 `PluginInventoryLocaleKey`（即 `locales.ts` 中 `zh` 字典的 key 联合）
- `locales.ts` 中定义的键为 `cordis`（值为 `'Cordis 状态'` / `'Cordis status'`），不是 `'@deepseek-ai/cordis'`
- 包名 `@deepseek-ai/cordis` 被误用为 locale key
- **影响**: 阻塞 typecheck 和 build；运行时 `t('@deepseek-ai/cordis')` 会返回未翻译的原始字符串
- **修复**: 将 `t('@deepseek-ai/cordis')` 改为 `t('cordis')`

### 1.2 修复方案

```diff
- <dt>{t('@deepseek-ai/cordis')}</dt>
+ <dt>{t('cordis')}</dt>
```

## 2. Lint `pnpm run lint` — ✅ PASS

exit code 0，无 lint 错误。

## 3. 构建 `pnpm run build` — ❌ FAIL（同 #1）

构建包含 `tsc -b tsconfig.client.json`，与 typecheck 共享同一类型错误。修复 typecheck 即可。

## 4. 克隆检测 `pnpm run duplication` — ✅ PASS

0 克隆，1118 文件 / 241117 行 / 1213342 token 分析完成。

## 5. 单元测试 `pnpm run test` — ⚠️ 部分完成

由于小内存机器资源限制，测试套件在运行约 10 分钟后被超时中断。已捕获的失败用例：

| 失败用例 | 首次失败原因 | 归因 |
|---|---|---|
| `typert/generator/tests/tools-catalog.spec.ts` | 48s 超时 | 环境慢（小内存 + 并行编译） |
| `scripts/oxlint-contract.spec.ts`（×5） | 各种 5s 默认超时 | 环境慢（上次报告同样失败） |
| `test-support/acp-snapshot/tests/harness.spec.ts` | `waitForSubagentTurnEnd` 竞态 | 环境抖动 |
| `client/ui-primitives/code-block.client.spec.tsx` | 8.9s 超时 | 环境慢 |

**所有已见失败均为环境超时或竞态抖动，与上次报告（08-16）一致，无新增真实缺陷。**

## 6. 快照回放 `pnpm run test:snapshot` — ⚠️ 超时

测试套件运行超过 300 秒仍未完成，被外部超时中断。小内存机器上快照回放需要大量子进程（loader-smoke），资源不足导致无法在合理时间内完成。

## 7. 失败归因汇总

| 类别 | 数量 | 明细 |
|---|---|---|
| **真实缺陷**（需修复） | **1** | `@deepseek-ai/cordis` 误作 locale key（阻塞 typecheck + build） |
| **环境/硬件限制** | ~8 | 环境超时（oxlint-contract ×5, tools-catalog ×1, code-block ×1, acp-harness ×1） |
| **资源不足** | 1 | 快照回放在小内存机器上超时 |

## 8. 建议

1. **合入前必修**: 修复 `PluginInventorySettingsTab.tsx` 第 181 行 `t('@deepseek-ai/cordis')` → `t('cordis')`。
2. 本机为小内存机器，单元测试和快照测试的超时类失败为已知环境限制，CI 上不会触发。
3. 与上次报告（08-16）相比，staged 的 `apps/desktop` 变更已不存在，lint/knip/文档配对问题已消除。

## 附：复现命令

```sh
pnpm run typecheck
pnpm run lint
pnpm run build
pnpm run duplication
pnpm run test
pnpm run test:snapshot
```
