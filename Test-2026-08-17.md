# DeepSeek Harness — 测试报告

- **日期**: 2026-08-17
- **环境**: Linux x64, 4 核 / 3.8 GiB RAM, Node v22.23.2, pnpm 11.7.0, vitest v4.1.8
- **分支**: master (`cc1921a2af`)

## 一、检查结果总览

| 检查项 | 命令 | 结果 |
|---|---|---|
| TypeCheck | `pnpm run typecheck` | ✅ PASS |
| Lint | `pnpm run lint` | ✅ PASS |
| Build | `pnpm run build` | ✅ PASS (lib + website) |
| Duplication | `pnpm run duplication` | ✅ PASS (0 clones) |
| Unit Tests | `pnpm run test` | ⚠️ 10 failures — 全部为环境超时 |

## 二、单元测试失败详情

全部 10 个失败均为**环境资源不足**（4 核 / 3.8 GiB RAM）导致的超时，不是代码缺陷。

| 测试文件 | 失败数 | 原因 |
|---|---|---|
| `scripts/oxlint-contract.spec.ts` | 7 | vitest worker pool 在低内存下启动缓慢 (167s)，独立运行 PASS |
| `packages/typert/generator/tests/tools-catalog.spec.ts` | 1 | vitest 启动超时 (65s)，独立运行 PASS |
| `packages/test-support/acp-snapshot/tests/harness.spec.ts` | 1 | vitest 启动超时 (84s)，独立运行 PASS |
| `scripts/install-lefthook.spec.ts` | 1 | vitest 启动超时 (192s)，独立运行 PASS |
| `packages/client/ui-primitives/tests/code-block.client.spec.tsx` | 1 | 23 个 lazy grammar 动态 import 在低内存下超过 5000ms 默认超时 |

## 三、E2E CI 失败

GitHub Actions 上的 E2E workflow 失败原因是仓库未配置 `DEEPSEEK_API_KEY_EXTERNAL` secret，导致 preflight 阶段拒绝运行。这不是代码缺陷。

**解决方法**: 在仓库 Settings → Secrets → Actions 中配置 `DEEPSEEK_API_KEY_EXTERNAL`。

## 四、结论

| 类别 | 数量 |
|---|---|
| 真实代码缺陷 | **0** |
| 环境超时失败 | **10** |
| CI 配置缺失 | **1** (E2E secret) |

**本报告无需修复操作。** 所有失败均为 4 核 / 3.8 GiB RAM 的环境限制，CI 标准环境（ubuntu-latest, 7GB RAM）可正常通过。
