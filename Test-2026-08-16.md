# DeepSeek Harness 全仓库测试报告

- **日期**: 2026-08-16
- **分支**: master（`47f943859b`，含未提交的 `apps/desktop` staged 变更，34 个新增文件 + 7 个修改文件）
- **测试范围**: 全仓库测试套件（用户指定）
- **执行方式**: 顺序执行各门禁；失败用例逐个隔离重跑以区分「环境抖动 / 版本差异 / 真实缺陷」

## 测试环境

| 项 | 值 |
|---|---|
| 操作系统 | Linux |
| Node.js | v22.23.2（在 engines `^22.19 \|\| >=24` 范围内） |
| pnpm | 11.7.0 |
| 硬件 | 4 核 CPU / 3.8 GiB 内存（全量并行时负载 4.3–6.1） |
| Locale | `zh_CN.UTF-8`（CI 为 en 系，见 §3.3） |
| DEEPSEEK_API_KEY | 未设置（e2e 真 API 套件自动跳过） |
| CI 对照 | CI `PRIMARY_NODE_VERSION=24`（`.github/workflows/ci.yml`），本机 Node 22 |

> 本机为 4 核 3.8 GiB 的小内存机器，全量 vitest 并行会 spawn 大量 fork；以下多项「超时」类失败为负载/事件循环延迟所致，已在隔离重跑中通过。CI 硬件与 Node 24 下不会触发。

## 门禁结果总览

| 门禁 | 命令 | 结果 | 失败数 | 归因 |
|---|---|---|---|---|
| 单元测试 | `pnpm run test` | ⚠️ FAIL | 7 / 13422 | 3 负载抖动 + 2 环境慢超时 + 1 真实 locale 缺陷 |
| 覆盖率门禁 | `pnpm run test:coverage` | ⚠️ FAIL | 8 / 13421 | 7 插桩下环境超时 + 1 locale 缺陷（阈值未及评估） |
| 类型检查 | `pnpm run typecheck` | ✅ PASS | 0 | — |
| Lint | `pnpm run lint` | ❌ FAIL | **16（全部在 staged 的 apps/desktop）** | **变更归因** |
| 构建 | `pnpm run build` | ✅ PASS | 0 | — |
| 仓库卫生 | `pnpm run hygiene` | ❌ FAIL | rescope-vendor 26 处 + knip 6 项 | 既有问题 + **变更归因（knip）** |
| 克隆检测 | `pnpm run duplication` | ✅ PASS | 0 克隆 | — |
| 文档门禁 | `pnpm run doc-sync` | ❌ FAIL | 27/28 通过，1 失败 | **变更归因（翻译配对）** |
| 快照回放 | `pnpm run test:snapshot` | ⚠️ FAIL | 6 / 116 | 3 负载抖动 + 2 Node 版本差异 + 1 性能敏感 |
| e2e | `pnpm run test:e2e` | ⚠️ FAIL | 1 / 198 | PTY 信号时序抖动（隔离通过） |

**结论**: 无构建/类型/克隆问题。staged 变更本身的功能测试全部通过（desktop 单测 26/26、desktop smoke e2e 通过），但存在 **3 类变更归因的门禁失败**（lint 16 错、knip 6 项、文档翻译配对 1 处）需修复；其余失败均为本机环境（负载、Node 22、zh_CN locale）或既有问题。

---

## 1. 单元测试 `pnpm run test` — FAIL（7 失败）

815 文件 / 13422 通过 / 7 失败 / 109 跳过。

失败用例及隔离重跑结论：

| 失败用例 | 首次失败原因 | 隔离重跑 |
|---|---|---|
| `subprocess-local/tests/process-exit.spec.ts` | ENOENT 临时文件（竞争） | ✅ 通过（负载抖动） |
| `test-support/acp-snapshot/tests/harness.spec.ts` | waitFor 超时（竞争） | ✅ 通过（负载抖动） |
| `typert/generator/tests/tools-catalog.spec.ts` | 30s 超时 | ✅ 通过（负载抖动） |
| `scripts/oxlint-contract.spec.ts`（×2） | 5s 默认超时（实际 6.4s） | ✅ 放宽 `--testTimeout=30000` 通过（环境慢） |
| `client/ui-primitives/code-block.client.spec.tsx` | 5s 默认超时（实际 5.04s） | ✅ 放宽超时通过（环境慢） |
| `shell/bash-sandbox/tests/sandbox.spec.ts` | `denied` 期望 true 实得 false | ❌ **确定性失败，真实缺陷**（见 §3.1） |

### 1.1 真实缺陷：bash-sandbox 拒绝分类依赖英文 locale

- 复现：`echo x > <chmod 555 目录>/f` 的 stderr 在 `zh_CN.UTF-8` 下为 `bash: ...: 权限不够`，拒绝签名匹配的是英文子串 `permission denied` / `read-only file system` → 分类器漏判 → `sandbox.denied: false`（错误事实）。
- 验证：`LC_ALL=C` 下重跑该用例 **通过**。
- 影响：**非英文系统的真实用户**在沙箱拒绝场景会收到错误的 `denied` 事实（影响 tool-bash 的 `[sandbox: file access denied ...]` 提示与升级建议逻辑）。
- 与本次 staged 变更无关，属既有问题。建议在 spawn 的 bash 上强制 `LC_ALL=C` 或扩展拒绝签名集。

## 2. 覆盖率门禁 `pnpm run test:coverage` — FAIL（8 失败）

含 `--retry=1` 重试后仍失败 8 个（6 文件）：bash-sandbox locale（§1.1 同一缺陷）+ 7 个**插桩下的环境超时**（`oxlint-contract`、`code-block`、`acp-snapshot`、`tools-catalog`、`type-model`×2、`gen-client-catalog`），其中 `type-model` 在 180s 超时内仍未完成——v8 插桩 + 4 核机器上 whole-workspace 编译器分析过慢。

**覆盖率阈值未及评估**：vitest 在测试失败时不会输出覆盖率表（日志中无阈值报告）。typert generator 源码按设计已排除在覆盖率 include 之外（`vitest.config.ts` 注释说明），100% 每文件阈值配置保持不变。

## 3. 快照回放 `pnpm run test:snapshot` — FAIL（6 失败）

| 失败用例 | 原因 | 隔离重跑 |
|---|---|---|
| `apps/cli/dsh-badge.snapshot.ts` | 30s loader-smoke 超时 | ✅ 通过（负载抖动） |
| `headless-agent/headless.snapshot.ts`（×2） | 30s loader-smoke 超时 | ✅ 通过（负载抖动） |
| `acp-agent/goal.snapshot.ts`（×2） | 子进程 stderr 出现 `ExperimentalWarning: SQLite ...`，断言期望空 stderr | ❌ 确定性（Node 版本差异，见 §3.2） |
| `headless-agent/headless.snapshot.ts` "keeps provider comments alive" | mock server 收到 2 次请求（期望 1） | ❌ 确定性（性能敏感，见 §3.3） |

### 3.2 Node 22 vs CI Node 24：SQLite 实验警告污染 stderr

本机 Node 22.23.2 对 `node:sqlite` 输出 `ExperimentalWarning`（在 engines 支持范围内）；CI 用 Node 24（SQLite 已稳定，无警告），fixture 因此只在 Node 22 上失败。属「受支持 Node 22 与 CI 版本的差异」，非变更归因，但在支持范围内可复现。

### 3.3 `streamIdleTimeoutMs: 150` 在慢事件循环下触发 provider 重试

`deepseek-defaults.cordis.yml` 配置 `streamIdleTimeoutMs: 150`，mock 每 60ms 发 keep-alive 注释。本机事件循环延迟使块间隔 >150ms → `llm-deepseek` adapter 的 stream-idle 重试策略（`STREAM_IDLE_TIMEOUT_CODE`）重发请求 → mock 收到 2 次。3 次隔离运行均失败，属本机性能敏感，CI 硬件下不触发。

## 4. e2e `pnpm run test:e2e` — FAIL（1 失败）

61 文件 / 113 通过 / 84 跳过（无密钥自动跳过）/ 1 失败。

- `apps/cli/headless-shutdown.e2e.ts`「第二次 Ctrl+C 强制退出」：PTY 信号时序测试，`exited 124`、3 次尝试（含 2 次自动重试）均超时；**隔离重跑通过** → 负载下 PTY 时序抖动。
- ✅ **`apps/desktop/tests/desktop-smoke.e2e.ts` 通过**：启动真实构建的 web runtime、HTTP 就绪、干净退出（见 §6）。

## 5. 其余门禁

| 门禁 | 结果 | 说明 |
|---|---|---|
| `pnpm run typecheck` | ✅ PASS | 全仓 tsc（含 `apps/desktop`） |
| `pnpm run build` | ✅ PASS | 全部产物生成，含 `apps/desktop/lib/` |
| `pnpm run duplication` | ✅ PASS | 0 克隆 |
| `pnpm run hygiene` | ❌ FAIL | 见下 |

### 5.1 hygiene 分解

- ❌ `rescope-vendor:check`：26 处 pre-rescope 名称 token 残留，**全部位于非 staged 文件**（`packages/extensions/*`、`docs/*`、`scripts/gen-cordis-catalog.ts` 等）→ **既有问题**（HEAD 上已存在，与 desktop 变更无关）。该门禁短路导致后续子门禁未执行，已单独逐个补跑。
- ❌ `knip`（单独补跑）：**6 项问题全部指向 staged 的 apps/desktop**（见 §6.2）。
- ✅ 其余 9 项子门禁（publint、constraints、licenses、invariants、built-invariants、cordis-config、node-next-types、runtime-closure、vendored-links）**全部通过**。

## 6. Staged 变更（`apps/desktop`）专项结论

### 6.1 功能测试 ✅

| 测试 | 结果 |
|---|---|
| desktop 单元测试（config/ports/ready/launch，4 文件 26 用例） | ✅ 全部通过 |
| desktop smoke e2e（真实 web runtime 启动/就绪/退出） | ✅ 通过 |
| typecheck / build | ✅ 通过 |

### 6.2 变更归因的门禁失败（需修复后合入）

1. **lint：16 个错误，全部在 apps/desktop**：
   - `no-confusing-void-expression` ×10（`main.ts`×4、`ports.ts`×2、`launch.ts`×2、`ready.spec.ts`×2、`ports.spec.ts`×2、`launch.spec.ts`×1）
   - `no-unnecessary-condition` ×3（`main.ts`×2、`launch.ts`×1）
   - `no-unsafe-assignment` ×1（`launch.spec.ts:46`）
2. **knip：6 项**：
   - 未使用文件：`src/bin.ts`、`tests/desktop-smoke.e2e.ts`
   - 未使用 devDependency：`execa`（`package.json`，源码用 `node:child_process`）
   - 未使用导出类型：`StreamLike`、`SpawnOptions`（`launch.ts`）、`ProbeResult`（`ports.ts`）
3. **doc-sync 翻译配对**：`apps/desktop/README.md` 锚点 `#configuration` 与 `README.zh.md` 锚点 `#配置` 不一致（27/28 门禁通过，仅此失败）。

> 注：`bin.ts` 在 `package.json` 的 `bin` 字段中被引用，knip 是否覆盖 apps 目录属 knip 配置问题，需作者判断是补配置还是清理代码。

## 7. 失败归因汇总

| 类别 | 数量 | 明细 |
|---|---|---|
| **变更归因**（desktop，需修复） | 3 类 | lint 16 错；knip 6 项；文档翻译配对 1 处 |
| **真实既有缺陷**（与变更无关） | 2 类 | bash-sandbox locale 依赖；rescope-vendor 26 处残留 |
| **环境/版本差异** | 3 类 | Node 22 SQLite 警告（2 用例）；stream idle 性能敏感（1 用例）；负载/慢机器超时抖动（约 14 用例，隔离重跑全部通过） |

## 8. 建议

1. **合入前必修**：修复 apps/desktop 的 16 个 lint 错误、knip 6 项、README 锚点不一致（§6.2）。
2. **建议跟进**：bash-sandbox 的 locale 依赖缺陷（§1.1）会影响中文系统真实用户；`rescope-vendor` 26 处残留阻塞 hygiene 门禁（可考虑单独 PR 修复）。
3. 本机为小内存机器，全量测试建议在 CI 或更强硬件上执行以获得干净的阈值评估；覆盖率阈值是否达标需在一次零失败插桩运行后确认。
4. 快照门禁在 Node 22 上的 SQLite 警告（§3.2）可考虑让 normalizer 剥离该警告，以兑现 engines 对 Node 22 的支持承诺。

## 附：复现命令

```sh
pnpm run test                 # 单元测试
pnpm run test:coverage        # 覆盖率门禁
pnpm run typecheck
pnpm run lint
pnpm run build
pnpm run hygiene              # 短路于 rescope-vendor；子门禁单独补跑见 §5.1
pnpm run duplication
pnpm run doc-sync
pnpm run test:snapshot        # 无密钥回放
pnpm run test:e2e             # 真 API 套件无密钥自跳过；desktop smoke 无需密钥

# locale 缺陷复现/验证
LC_ALL=C pnpm exec vitest run packages/shell/bash-sandbox/tests/sandbox.spec.ts -t "reports a real permission failure"
```
